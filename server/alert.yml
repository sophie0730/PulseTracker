# Config your alerting ruls
# No need to fill data for rules and annotations columns 
groups: # groups should be entered once
- name: # alert title
  rules:
  - alert: # alert name
    expr: #the alerting rule
    for:  # the duration for the alert
    annotations:
      summary: